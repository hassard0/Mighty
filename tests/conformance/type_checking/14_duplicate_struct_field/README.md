# 14 duplicate_struct_field

Positive-fire for **MT2014 DUPLICATE_STRUCT_FIELD**. Spec v1.0-RC §6 (types).

The struct literal lists `x` twice. The type checker reports MT2014
on the second occurrence.
