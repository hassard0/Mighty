# 02 arity_mismatch

Positive-fire for **MT6002 MACRO_ARITY_MISMATCH**. Spec v1.0-RC §20 (macros).

The `one` macro declares one parameter; the call site supplies two.
The preprocessor reports MT6002.
