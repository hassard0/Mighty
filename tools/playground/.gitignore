node_modules/
build/
dist/
.vite/
*.log

# v0.35 T1 — wasm-pack output. Rebuilt by the GH Pages workflow
# (.github/workflows/pages.yml) before every deploy, so we don't
# need it in the source tree. `tools/playground/public/wasm/` is
# kept as an empty directory via `public/wasm/.gitkeep`.
public/wasm/mty_cli.js
public/wasm/mty_cli.d.ts
public/wasm/mty_cli_bg.wasm
public/wasm/mty_cli_bg.wasm.d.ts
public/wasm/package.json

# Playwright test artifacts.
test-results/
playwright-report/
