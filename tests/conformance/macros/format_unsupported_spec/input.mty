// v0.25 Track D negative: indexed positional `{0}` still not supported
// (deferred to v0.26 along with dynamic width via arg `{:1$}`).
// v0.25 SHIPPED width/zero-pad/align/precision/sign/alt-form so the
// original v0.24 `{:05}` example now expands successfully — the
// indexed-positional shape replaces it as the smoke fixture for
// MT6010 (`unsupported spec`).
fn main() {
  let n = 42
  log(format!("{0}", n))
}
