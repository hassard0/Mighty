// Slice-7 honors deadline only between turns, not inside one (per A41).
// The positive-firing path is slice-8 scope; this case is #[ignore]d.
protocol P {
  Hit() -> Str
}

agent A: P {
  on Hit() -> "ok"
}

fn main() {
  let h = spawn A()
  let r = h?Hit() @1s
  log(r)
}
