extern {
  fn work(item: I32) -> Unit!WorkErr
  fn ready() -> Bool
  fn step() -> Unit
  fn tick() -> Unit
}

fn process(items: &[I32]) -> Unit!WorkErr {
  for item in items {
    work(item)?
  }
  while ready() {
    step()
  }
  loop {
    tick()
  }
}
