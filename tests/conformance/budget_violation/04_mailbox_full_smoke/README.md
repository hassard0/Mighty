# 04 mailbox_full_smoke

`mb 1k` declaration inside a budget block. Locks in the grammar
across parse → HIR → MtyIR. MT5012 mailbox_full firing belongs to the
tokio runtime (Slice-7 / Slice-8). Spec §16.2.
