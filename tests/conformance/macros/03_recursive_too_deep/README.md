# 03 recursive_too_deep

Positive-fire for **MT6004 RECURSIVE_MACRO_TOO_DEEP**. Spec v1.0-RC §20 (macros).

The `r` macro calls itself unconditionally. Expansion stops at the
v0.6 depth cap (32) with MT6004.
